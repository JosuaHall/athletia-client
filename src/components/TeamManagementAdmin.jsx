import PropTypes from "prop-types";
import React, { Component } from "react";
import BackButton from "./BackButton";

class TeamManagementAdmin extends Component {
  render() {
    return (
      <div>
        <BackButton />
        TeamManagementAdmin
      </div>
    );
  }
}

export default TeamManagementAdmin;
